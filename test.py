import numpy as np
import math

def scan(pos, scanRange, board):

                # print(f"scanning {[pos[0] -xOffset, pos[1] - yOffset]}") 

    
    return board

board = np.empty([10, 10])
for i in range(10):
    for k in range(10):
        board[i][k] = 0
scan([5, 5], 4, board)
board[5, 5] = 8
print(board)